---
- name: "Common :: Disable Autoupdate Services"
  service:
    name: "{{ item }}"
    state: stopped
    enabled: no
    daemon_reload: yes
  loop: "{{ autoupdate_services }}"
  when:
    - autoupdate_services is defined
